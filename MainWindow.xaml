<Window x:Class="ArisHotel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Отель Арис — Главная" Height="600" Width="1000" WindowStartupLocation="CenterScreen">
    <Grid Background="#FFECF0F1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header with user info -->
        <StackPanel Grid.Row="0" Margin="10">
            <TextBlock x:Name="txtWelcome" FontSize="16" FontWeight="Bold" TextAlignment="Left"/>
        </StackPanel>
        
        <!-- Navigation and Add button -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Navigation buttons -->
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button x:Name="btnRoles" Content="Роли" Width="80" Margin="0,0,5,0" Click="BtnNavigation_Click"/>
                <Button x:Name="btnUsers" Content="Пользователи" Width="100" Margin="0,0,5,0" Click="BtnNavigation_Click"/>
                <Button x:Name="btnRooms" Content="Номера" Width="80" Margin="0,0,5,0" Click="BtnNavigation_Click"/>
                <Button x:Name="btnBookings" Content="Бронирования" Width="100" Margin="0,0,5,0" Click="BtnNavigation_Click"/>
            </StackPanel>
            
            <!-- Add button -->
            <Button x:Name="btnAdd" Grid.Column="1" Content="Добавить" Width="100" Click="BtnAdd_Click"/>
        </Grid>
        
        <!-- DataGrid -->
        <DataGrid x:Name="dataGrid" Grid.Row="2" Margin="10" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  CanUserDeleteRows="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  SelectionMode="Single"
                  SelectionUnit="FullRow">
            <DataGrid.Columns>
                <!-- Dynamic columns will be added in code-behind -->
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- Footer with logout -->
        <StackPanel Grid.Row="3" HorizontalAlignment="Right" Margin="10">
            <Button Content="Выйти" Width="100" Click="BtnLogout_Click"/>
        </StackPanel>
    </Grid>
</Window>
